import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;

  width: 100%;
  height: 60px;

  border-bottom: 1px solid black;
`;

const Header = ({ type }) => {
  return <Wrapper>땡땡땡땡</Wrapper>;
};

export default Header;
